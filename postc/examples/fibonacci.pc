# PostC Example: Fibonacci Sequence
# Demonstrates recursion

:fib 1 param
  dup 2 < if
    # Base case: return the number itself (0 or 1)
  else
    # Recursive case: fib(n-1) + fib(n-2)
    dup 1 - fib
    swap 2 - fib
    +
  ;
;

"Calculating Fibonacci numbers:" print
5 fib print